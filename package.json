{
  "name": "abhigna-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Abhigna Kandala's portfolio: zero-dependency TypeScript front end plus a retrieval-augmented assistant that needs no API key.",
  "type": "module",
  "scripts": {
    "build:knowledge": "node scripts/build-knowledge.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "build:ts": "tsc -p tsconfig.json",
    "build": "npm run build:knowledge && npm run build:pages && npm run build:ts",
    "build:api-dev": "tsc -p api/tsconfig.dev.json",
    "dev": "npm run build && npm run build:api-dev && node scripts/dev-server.mjs",
    "check": "tsc -p tsconfig.json --noEmit && tsc -p api/tsconfig.json --noEmit",
    "test:answers": "npm run build:knowledge && npm run build:api-dev && node scripts/test-answers.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
