{
  "name": "abhigna-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Abhigna Kandala's portfolio: zero-dependency TypeScript front end plus a Vercel Function that powers a retrieval-augmented AI concierge.",
  "type": "module",
  "scripts": {
    "build:knowledge": "node scripts/build-knowledge.mjs",
    "build:ts": "tsc -p tsconfig.json",
    "build": "npm run build:knowledge && npm run build:ts",
    "dev": "npm run build && npx serve public -l 5173",
    "check": "tsc -p tsconfig.json --noEmit && tsc -p api/tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
